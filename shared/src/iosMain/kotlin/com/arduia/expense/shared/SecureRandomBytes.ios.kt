package com.arduia.expense.shared

/**
 * iOS-readiness stub (not implemented yet). Implement with `SecRandomCopyBytes` (Security
 * framework) when the iosApp phase starts.
 */
actual fun secureRandomBytes(size: Int): ByteArray {
    TODO("iOS secure random not implemented yet — use SecRandomCopyBytes in the iosApp phase")
}
