package com.arduia.expense.shared

/**
 * iOS-readiness stub (not compiled yet — no iOS target is configured). Implement with
 * NSDate().timeIntervalSince1970 when the iosApp phase starts.
 */
actual fun currentEpochMillis(): Long {
    TODO("iOS clock not implemented yet — use NSDate in the iosApp phase")
}
