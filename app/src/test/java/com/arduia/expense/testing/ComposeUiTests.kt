package com.arduia.expense.testing

/**
 * Marker for Robolectric Compose UI tests that launch an Activity via the compose
 * ui-test-manifest. That manifest is only merged into the debug variant, so these tests
 * are excluded from the release unit-test variant (see app/build.gradle.kts).
 */
interface ComposeUiTests
