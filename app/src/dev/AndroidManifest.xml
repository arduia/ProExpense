<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Dev-only launcher for the UI verification catalog. Not present in production builds. -->
    <application>
        <activity
            android:name="com.arduia.expense.ui.catalog.UiCatalogActivity"
            android:exported="true"
            android:label="Pro Expense UI Catalog"
            android:theme="@style/Base.Theme.ProExpense"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
